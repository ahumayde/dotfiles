# 1. Change directory and cd into telescope.nvim 
# 2. Checkout to the master branch

# full command:
cd ~/.local/share/nvim/site/pack/packer/start/telescope.nvim/ && git checkout master && git pull
cd -
